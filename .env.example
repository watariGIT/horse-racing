# GCP Configuration
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# JRA API
JRA_API_KEY=your-jra-api-key-here

# Environment (dev / prod)
ENVIRONMENT=dev

# BigQuery
BQ_DATASET=horse_racing

# GCS Buckets (auto-generated from project ID if not set)
# GCS_BUCKET_RAW=your-project-raw-data
# GCS_BUCKET_PROCESSED=your-project-processed
# GCS_BUCKET_MODELS=your-project-models
