# Development environment overrides

gcp:
  project_id: ${GCP_PROJECT_ID}

bigquery:
  dataset: horse_racing_dev

mlflow:
  tracking_uri: "gs://${GCP_PROJECT_ID}-models/mlruns"

logging:
  level: DEBUG
  format: text
